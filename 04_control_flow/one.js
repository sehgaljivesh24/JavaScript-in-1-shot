// Jab Website mein loggedIn ho - toh alag code run hona chaiye
// Jab logged out hain - toh alag code run  hona chaiye

// if
// if (condition) {
//     // condition must be true - if we need to execute code inside if
// }

const isUserLoggedIn = true;
if (isUserLoggedIn) {
  console.log(`The User is logged in`);
}

// <, >, <=, >=, !=, ==, ===